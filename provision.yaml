#!/usr/bin/env ansible-playbook

- name: provision

  hosts:
    - fed143

  vars:
    ansible_user: ansible

  roles:
  - auth
  - firewall
  - packages
  - role: httpd
    when: "'httpd' in group_names"
  - role: dehydrated
    when: "'dehydrated' in group_names"
  #  - role: gitea
    #when: "'gitea' in group_names"

